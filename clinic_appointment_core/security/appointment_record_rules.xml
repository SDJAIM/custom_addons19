<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ======================== -->
    <!-- Record Rules for Appointments -->
    <!-- ======================== -->

    <!-- Appointment Manager Rule - Managers and Admins see all appointments -->
    <record id="appointment_manager_rule" model="ir.rule">
        <field name="name">Appointment: Manager/Admin All Access</field>
        <field name="model_id" ref="model_clinic_appointment"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_appointment_manager')), (4, ref('group_appointment_admin'))]"/>
    </record>

    <!-- Appointment User Rule - Users see all appointments (can be customized later) -->
    <record id="appointment_user_rule" model="ir.rule">
        <field name="name">Appointment: User Access</field>
        <field name="model_id" ref="model_clinic_appointment"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_appointment_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Patient Portal Appointments Rule - Portal users see only their own appointments -->
    <record id="appointment_portal_rule" model="ir.rule">
        <field name="name">Appointment: Portal Patient Access</field>
        <field name="model_id" ref="model_clinic_appointment"/>
        <field name="domain_force">[('patient_id.partner_id', '=', user.partner_id.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>
</odoo>
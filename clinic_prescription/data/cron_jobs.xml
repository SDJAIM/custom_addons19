<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- CRON JOBS TEMPORARILY DISABLED FOR DEPLOYMENT -->
        <!-- These will be configured via UI or created programmatically once methods are properly implemented -->
        <!--
        Cron Specifications (to be manually created):
        1. Check Expiring Medications - interval: 1 day - executes clinic.medication::check_expiring_lots(days_before=30)
        2. Check Medication Reorder Levels - interval: 6 hours - executes clinic.medication::check_reorder_levels()
        3. Check Expired Prescriptions - interval: 1 day - executes clinic.prescription::check_expired_prescriptions()
        4. Prescription Refill Reminders - interval: 1 day - executes clinic.prescription.line::check_refill_reminders()
        -->

    </data>
</odoo>
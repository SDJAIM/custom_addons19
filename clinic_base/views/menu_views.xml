<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Main Clinic Root Menu -->
        <menuitem id="menu_clinic_root"
                  name="Clinic"
                  sequence="5"
                  web_icon="clinic_base,static/description/icon.png"/>

        <!-- Integrations Menu -->
        <menuitem id="menu_clinic_integrations"
                  name="Integrations"
                  parent="menu_clinic_root"
                  sequence="70"/>

        <!-- System Configuration Menu -->
        <menuitem id="menu_clinic_system"
                  name="Configuration"
                  parent="menu_clinic_root"
                  sequence="80"
                  groups="group_clinic_base_manager"/>

        <!-- Audit Logs -->
        <menuitem id="menu_clinic_audit"
                  name="Audit &amp; Logs"
                  parent="menu_clinic_system"
                  sequence="10"/>

        <!-- Temporarily disabled until audit_log_views is enabled
        <menuitem id="menu_clinic_audit_logs"
                  name="Audit Logs"
                  parent="menu_clinic_audit"
                  action="action_clinic_audit_log"
                  sequence="10"/> -->
    </data>
</odoo>
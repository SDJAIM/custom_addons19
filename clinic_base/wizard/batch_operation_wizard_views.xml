<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Form View -->
        <record id="view_clinic_batch_operation_wizard_form" model="ir.ui.view">
            <field name="name">clinic.batch.operation.wizard.form</field>
            <field name="model">clinic.batch.operation.wizard</field>
            <field name="arch" type="xml">
                <form string="Batch Operation">
                    <group>
                        <field name="operation_type"/>
                        <field name="model_name"/>
                        <field name="record_count" readonly="1"/>
                    </group>
                    <footer>
                        <button name="action_execute" type="object" string="Execute" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Action -->
        <record id="action_clinic_batch_operation_wizard" model="ir.actions.act_window">
            <field name="name">Batch Operations</field>
            <field name="res_model">clinic.batch.operation.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>